{
  clickgen,
  hyprcursor,
  lib,
  python3,
  python3Packages,
  resvg,
  source,
  stdenvNoCC,
  baseColor ? "#282828",
  outlineColor ? "#ebdbb2",
  cursorThemeName ? "GoogleDot-Nixporn",
}:
stdenvNoCC.mkDerivation {
  pname = cursorThemeName;
  version = "unstable";
  src = source;

  nativeBuildInputs = [
    clickgen
    hyprcursor
    python3
    python3Packages.tomli
    python3Packages.tomli-w
    resvg
  ];

  buildPhase = ''
    runHook preBuild

    cp -R svg themed-svg
    chmod -R u+w themed-svg
    find themed-svg/static -type f -name '*.svg' -exec sed -i \
      -e 's/#00FF00/${baseColor}/g' \
      -e 's/#0000FF/${outlineColor}/g' {} +

    mkdir -p bitmaps
    cp -R bitmaps/GoogleDot-Black/. bitmaps/
    while IFS= read -r svgFile; do
      resvg "$svgFile" "bitmaps/$(basename "$svgFile" .svg).png"
    done < <(find themed-svg/static -type f -name '*.svg' | sort)

    ctgen build.toml \
      --platforms x11 \
      --bitmaps-dir bitmaps \
      --out-dir xcursor-theme \
      --theme-name '${cursorThemeName}' \
      --theme-comment 'GoogleDot cursors generated by nixporn'

    mkdir -p hyprcursor-work/hyprcursors hyprcursor-theme
    cat > hyprcursor-work/manifest.hl <<'EOF'
    name = ${cursorThemeName}
    description = GoogleDot cursors generated by nixporn
    version = 1
    cursors_directory = hyprcursors
    EOF

    python ${../cursors/configure.py} \
      build.toml \
      bitmaps \
      hyprcursor-work/hyprcursors \
      200

    hyprcursor-util \
      --create hyprcursor-work \
      --output hyprcursor-theme \
      > /dev/null

    runHook postBuild
  '';

  installPhase = ''
    runHook preInstall

    install -dm 0755 "$out/share/icons/${cursorThemeName}"
    cp -R "xcursor-theme/${cursorThemeName}/." "$out/share/icons/${cursorThemeName}/"
    cp -R "hyprcursor-theme/theme_${cursorThemeName}/." "$out/share/icons/${cursorThemeName}/"

    runHook postInstall
  '';

  passthru = {
    inherit cursorThemeName;
  };

  meta = {
    description = "Configurable GoogleDot theme with XCursor and Hyprcursor support";
    homepage = "https://github.com/ful1e5/Google_Cursor";
    license = lib.licenses.gpl3Plus;
    platforms = lib.platforms.linux;
  };
}
